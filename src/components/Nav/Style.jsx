import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const NavFullWrapper = styled(Box)({
  width: "100%",
  height: 100,
});
export const NavImageWrapper = styled(Box)({
  width: "20%",
  height: "100%",
  display: "flex",
  justifyContent: "end",
  marginTop: 10,
});
export const Navimg = styled("img")({
  // width: "100%",
  // justifyContent: "center",
  marginTop: 10,
  marginBottom: 10,
});
